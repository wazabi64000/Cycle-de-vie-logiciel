services:
  wazabycode:
    build:
      context: ..
      dockerfile: lms/Dockerfile
    ports:
      - "3000:3000"
    environment:
      - NODE_ENV=production
      - PORT=3000
    restart: unless-stopped
